import React from 'react';
import {Text} from 'react-native';

import {StrConstants} from '../constants';

export const MedicationDetails = () => {
  return <Text>{StrConstants.medicationDetails}</Text>;
};
